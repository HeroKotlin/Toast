<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@drawable/custom_toast">

    <ImageView
            android:id="@+id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="@dimen/custom_toast_image_icon_margin_top"
            android:layout_marginBottom="0dp"
            android:padding="0dp"
            tools:src="@drawable/custom_toast_success"
            android:visibility="gone"
    />

    <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_margin="0dp"
            android:textColor="@color/custom_toast_text_color"
            android:textSize="@dimen/custom_toast_text_size"
            android:lineSpacingExtra="@dimen/custom_toast_text_line_spacing"
            android:maxLines="3"
            android:ellipsize="end"
            tools:text="你好"
    />

</LinearLayout>